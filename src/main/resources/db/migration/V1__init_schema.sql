CREATE TABLE entity
(
    id   int NOT NULL AUTO_INCREMENT,
    name varchar(255),
    PRIMARY KEY (id)
);

CREATE TABLE test_entity
(
    id   int NOT NULL AUTO_INCREMENT,
    name varchar(255),
    PRIMARY KEY (id)
);
